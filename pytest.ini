[pytest]
# Keep collection fast and scoped: don't let pytest wander into the frontend
# node_modules or the Python virtualenv.
testpaths = test_purple_recon.py
norecursedirs = .venv frontend backend node_modules dist
addopts = -q
