# Dev / CI dependencies for purple_recon.py and its test suite.
# (The deterministic test suite never invokes a real scan, so the nmap *binary*
#  is not required in CI — only the python-nmap wrapper module.)
rich>=13.0
python-nmap>=0.7.1
pytest>=8.0
ruff>=0.6

# Backend (FastAPI) — needed to import + test backend/ in CI.
fastapi>=0.110
pydantic>=2.0
