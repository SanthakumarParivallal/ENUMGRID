# Dev / CI dependencies for purple_recon.py and its test suite.
# (The deterministic test suite never invokes a real scan, so the nmap *binary*
#  is not required in CI — only the python-nmap wrapper module.)
rich>=13.0
python-nmap>=0.7.1
pytest>=8.0
pytest-cov>=5.0      # coverage measurement
hypothesis>=6.0      # property-based / fuzz testing of the parsers
httpx>=0.27          # FastAPI TestClient (backend API integration tests)
ruff>=0.6
bandit>=1.7          # static security analysis (SAST)
pip-audit>=2.7       # dependency CVE auditing

# Backend (FastAPI) — needed to import + test backend/ in CI.
fastapi>=0.110
pydantic>=2.0
reportlab>=4.0  # PDF report generation (test_report.py)
zeroconf>=0.130  # mDNS device discovery (test_mdns.py)

# Optional feature dependencies. In production the code degrades gracefully
# without them; in CI we install them so the credentialed/live code paths run
# under mocked I/O (no real network) and the 100% coverage gate is meaningful.
paramiko>=3.4     # credentialed SSH host facts        — backend/credscan.py
boto3>=1.34       # AWS cloud asset discovery          — backend/cloudscan.py
ldap3>=2.9        # Active Directory (LDAP) enumeration — backend/adscan.py
scapy>=2.5        # passive (zero-packet) discovery    — backend/passive.py
anthropic>=0.40   # AI copilot — Anthropic Claude       — backend/copilot.py
openai>=1.40      # AI copilot — OpenAI / Gemini / Ollama (shared SDK) — backend/copilot.py
#   matplotlib>=3.8   # benchmark recall/time plot — evaluation/benchmark.py --plot (plotting only)
